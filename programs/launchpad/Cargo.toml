[package]
name = "launchpad"
version = "0.1.0"
description = "X1Factory Launchpad — permissionless memecoin bonding curve + GigaSwap"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "launchpad"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []

[dependencies]
anchor-lang = { version = "0.28.0", features = ["init-if-needed"] }
anchor-spl = { version = "0.28.0", features = ["token", "associated_token"] }
bincode = "1.3.3"
solana-program = "=1.16.27"
